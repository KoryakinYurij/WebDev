# AGENTS.md — WebDev / MINDFIELD

## Обязательный контекст

- Рабочий проект: `D:\Code AI\Projects\WebDev\Test 1`.
- Перед дизайн-изменениями читать `docs/DESIGN-CODEX.md` и свежий хвост `docs/WORKLOG.md`; README — источник текущих gates и структуры.
- В working tree есть существенная незакоммиченная работа новее `HEAD`. Не делать `reset`, `clean`, checkout чужих файлов и не считать Git-историю текущим состоянием проекта.

## Быстрый рабочий цикл

- Для ориентации смотреть корень `depth=1`, затем только нужные `src/docs/tests`; `node_modules` не обходить без конкретной причины.
- Если нужный файл известен — читать его или нужный диапазон напрямую; поиск использовать, когда местоположение действительно неизвестно.
- Визуальные секции принимать по живому браузеру/скриншоту и измерениям, а не только по коду. Для серии проверок держать одну Playwright/Chromium-сессию.
- Во время итерации гонять только тесты изменяемой секции. Полный `npm run check` — один раз после законченного логического блока и перед commit/push.

## Решение по 002 — Протокол

- Выбрано направление **A**: превратить 002 в полноэкранную «камеру внимания», где большой canvas — главный экспонат, а текст работает как HUD.
- Сохранить полезную существующую механику: детерминированное поле, общий progress трёх фаз, pointer interaction, mobile/reduced-motion fallback и текущие регрессионные тесты.
- Три фазы должны различаться прежде всего композицией и поведением поля: сбор → давление/возвраты → расширение при сохранённом объекте.
- Не добавлять новый Three.js/библиотеку/архитектуру, если ту же задачу решает текущий canvas + GSAP.
- Текущее состояние: вариант A реализован; при дальнейшей доводке сохранять полноэкранную камеру, HUD и горизонтальную шкалу как базовую композицию 002.
