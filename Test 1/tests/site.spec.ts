import { test, expect } from '@playwright/test'
import AxeBuilder from '@axe-core/playwright'
const open=async(page:any)=>{const errors:string[]=[];page.on('console',(m:any)=>m.type()==='error'&&errors.push(m.text()));page.on('pageerror',(e:any)=>errors.push(String(e)));await page.goto('/');return errors}
test('desktop',async({page})=>{await page.setViewportSize({width:1440,height:900});const errors=await open(page);await expect(page.getByRole('heading',{level:1})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy();expect(errors).toEqual([])})
test('mobile',async({page})=>{await page.setViewportSize({width:390,height:844});const errors=await open(page);await expect(page.getByRole('heading',{level:1})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy();expect(errors).toEqual([])})
test('reduced motion skips Three',async({page})=>{await page.emulateMedia({reducedMotion:'reduce'});const urls:string[]=[];page.on('request',r=>urls.push(r.url()));await page.goto('/');await expect(page.getByRole('heading',{level:1})).toBeVisible();expect(urls.some(u=>u.includes('/src/three/hero-field'))).toBeFalsy()})
test('low quality',async({page})=>{await page.addInitScript(()=>localStorage.setItem('mindfield.motion',JSON.stringify({intensity:1,reduceOverride:false,quality:'low'})));await page.goto('/');await page.getByRole('button',{name:'Настройки движения и качества'}).click();await expect(page.getByRole('dialog')).toContainText('облегчённая сцена')})
test('axe serious/critical',async({page})=>{await page.emulateMedia({reducedMotion:'reduce'});await page.goto('/');const r=await new AxeBuilder({page}).analyze();expect(r.violations.filter(v=>v.impact==='serious'||v.impact==='critical')).toEqual([])})
